# latihan44_hivedatabase

A new Flutter project.

## Getting Started

- Create the model using @HiveType and create the field @HiveField(index)
- create monster.g.dart (flutter packages pub run build_runner build)